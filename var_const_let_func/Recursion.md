What is recursion? (Simple definition)

Recursion means a function calling itself.

This is allowed as long as it eventually stops.
If it never stops, JavaScript crashes because the call stack overflows.
